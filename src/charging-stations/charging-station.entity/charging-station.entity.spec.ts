import { ChargingStationEntity } from './charging-station.entity';

describe('ChargingStationEntity', () => {
  it('should be defined', () => {
    expect(new ChargingStationEntity()).toBeDefined();
  });
});
